# 服务端口
PORT=3000

# 前端地址（用于 CORS），uniapp 开发时可为 http://localhost:5173 或 微信开发者工具
# 多个用逗号分隔，* 表示允许所有
CORS_ORIGIN=*

# 上传文件保存目录（相对 server 目录）
UPLOAD_DIR=uploads

# 上传文件访问基础路径（如 http://localhost:3000/uploads）
BASE_URL=http://localhost:3000
